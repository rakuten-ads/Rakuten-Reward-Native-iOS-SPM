import XCTest
@testable import Rakuten_Reward_Native_iOS_SwiftPackage

final class Rakuten_Reward_Native_iOS_SwiftPackageTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(Rakuten_Reward_Native_iOS_SwiftPackage().text, "Hello, World!")
    }
}
